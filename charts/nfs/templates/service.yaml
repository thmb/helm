apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-service
  labels:
    app: nfs
spec:
  type: ClusterIP # default and internal service type
  clusterIP: {{ .Values.service.address }} # static internal address
  selector:
    app: nfs
  ports:
    - name: nfs
      port: 2049
