apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-service
  labels:
    app: minio
spec:
  type: NodePort # TODO: remove and change to ClusterIP (default)
  selector:
    app: minio
  ports:
    - name: api
      port: 9000
    - name: web
      port: 9090
