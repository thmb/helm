apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-secret
  labels:
    app: pgadmin
type: Opaque
data:
  username: {{ default "user@email.com" .Values.authentication.username | b64enc | quote }}
  password: {{ default "secret" .Values.authentication.password | b64enc | quote }}
