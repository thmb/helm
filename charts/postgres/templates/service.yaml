apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-service
  labels:
    app: postgres
spec:
  type: NodePort # TODO: remove and change to ClusterIP (default)
  selector:
    app: postgres
  ports:
    - port: 5432
