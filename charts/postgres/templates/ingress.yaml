apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Release.Name }}
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: {{ .Values.admin.host.name }}
    http:
      paths:
      - path: {{ .Values.admin.host.path }}
        pathType: Prefix
        backend:
          service:
            name: {{ .Release.Name }}-admin
            port:
              number: 80
