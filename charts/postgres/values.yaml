admin:
  image: dpage/pgadmin4:latest
  storage: standard # default class
  auth:
    email: user@email.com
    password: secret
  host:
    name: postgres.minikube
    path: /
  log:
    level: 10

database:
  image: postgres:latest
  storage: standard # default class
  auth:
    user: user
    password: secret
  default:
    name: test
