admin:
  image: dpage/pgadmin4:latest
  storage: manual # class
  auth:
    email: user@email.com
    password: secret
  host:
    name: postgres.minikube
    path: /
  log:
    level: 10

database:
  image: postgres:16.0
  storage: manual # class
  auth:
    user: user
    password: secret
  default:
    name: test
